export default function Componente03({ texto }) {
  return (
    <div
      style={{
        backgroundColor: "yellow",
        width: "300px",
        height: "300px",
        margin: "48px",
      }}
    >
      <p>{texto}</p>
    </div>
  );
}
